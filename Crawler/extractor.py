class Extractor:
    def __init__(self):
        super().__init__()

    @staticmethod
    def extractNumeric(filePath):
        with open (filePath, 'r', encoding="UTF-8"):
            print("g")

    @staticmethod
    def extractDateTime(filePath):
        with open (filePath, 'r', encoding="UTF-8"):
            print("g")

    @staticmethod
    def extractQuestions(filePath):
        with open (filePath, 'r', encoding="UTF-8"):
            print("g")

    @staticmethod
    def extractSpecialCharCount(filePath):
        with open (filePath, 'r', encoding="UTF-8"):
            print("g")
